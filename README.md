# WearU — Clothing Shop (static page)

Open `index.html` in your browser to view the clothing shop landing page.

Quick steps:

```bash
# from the repo root
xdg-open index.html  # or open index.html in your preferred browser
```

Files added:
- index.html — main static page
- styles.css — simple stylesheet
